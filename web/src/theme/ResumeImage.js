import styled from "styled-components";
import myResume from '../images/backgroundImage.png';

const ResumeBackground = styled.div`
  background: no-repeat url(${myResume});
`;

export default ResumeBackground